sudo: false

language: scala

before_cache:

cache:
  directories:

script:

before_deploy:

deploy:
  provider: script
  script: sbt "release with-defaults"
  on:
    condition: -e ./deploy.sbt
    all_branches: true
